.env
.venv/
__pycache__/
*.py[cod]
.idea/
.seeker/
slskd-data/
.coverage
build/
dist/
.DS_Store

# Roadmap item 81 (0.1) — packaging/build_dmg.py overwrites this with
# real build-local content (git SHA/describe, UTC timestamp) just
# before a real .dmg build. The committed version (GIT_SHA="dev") is
# the dev-run fallback; a real build's local modification to this
# tracked file should never itself be committed.
src/seeker/_build_info.py
